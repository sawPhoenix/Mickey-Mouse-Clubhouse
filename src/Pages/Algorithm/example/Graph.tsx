import React, { useState } from 'react';
import { Graph } from '../modal/Graph/Graph';
const GraphComponent = () => {
  const CC = (arr: []) => {
    let G = new Graph;
    let M
    
  }
  return (
    <div></div>
  )
}

export default GraphComponent